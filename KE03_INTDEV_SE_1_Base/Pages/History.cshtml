@page
@model HistoryModel
@{
    ViewData["Title"] = "Bestelgeschiedenis";
}

<h2>📜 Bestelgeschiedenis</h2>

@if (!Model.Bestellingen.Any())
{
    <div class="alert alert-info">Er zijn nog geen bestellingen geplaatst.</div>
}
else
{
    foreach (var bestelling in Model.Bestellingen.OrderByDescending(b => b.Tijdstip))
    {
        <div class="card mb-4">
            <div class="card-header">
                <strong>@bestelling.Voornaam @bestelling.Achternaam</strong> – @bestelling.Tijdstip.ToString("g")
            </div>
            <div class="card-body">
                <p>
                    <strong>Adres:</strong> @bestelling.Adres, @bestelling.Woonplaats<br />
                    <strong>Email:</strong> @bestelling.Email<br />
                    <strong>Telefoon:</strong> @bestelling.Telefoon<br />
                    <strong>Betaalmethode:</strong> @bestelling.Betaalmethode
                </p>

                <h5>🛒 Bestelde producten</h5>
                <table class="table table-sm">
                    <thead>
                        <tr>
                            <th>Product</th>
                            <th>Aantal</th>
                            <th>Prijs</th>
                            <th>Totaal</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in bestelling.BesteldeItems)
                        {
                            <tr>
                                <td>@item.Name</td>
                                <td>@item.Quantity</td>
                                <td>€@item.Price.</td>
                                <td>€@(item.Quantity * item.Price)</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="3" class="text-end"><strong>Totaal</strong></td>
                            <td>
                                <strong>
                                    €@bestelling.BesteldeItems.Sum(i => i.Quantity * i.Price)
                                </strong>
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    }
}